services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: youtube-note-maker-qdrant
    restart: unless-stopped
    ports:
      - "${QDRANT_HTTP_PORT:-2004}:6333"
      - "${QDRANT_GRPC_PORT:-2005}:6334"
    volumes:
      - qdrant_storage:/qdrant/storage

volumes:
  qdrant_storage:
